---
---

<section class="coming-soon section">
  <div class="container">
    <div class="coming-soon-inner">
      <p class="text-overline coming-soon-label">What's next</p>
      <h3 class="coming-soon-title">Gym Classes & Booking</h3>
      <p class="coming-soon-desc">
        Schedule classes, manage capacity, handle waitlists and cancellations.
        Coming 2026.
      </p>
    </div>
  </div>
</section>

<style>
  .coming-soon {
    background: var(--light-surface-1);
    padding-block: var(--space-9);
  }

  .coming-soon-inner {
    text-align: center;
  }

  .coming-soon-label {
    color: var(--teal-600);
    margin-bottom: var(--space-3);
  }

  .coming-soon-title {
    font-size: 20px;
    font-weight: 700;
    color: var(--navy-700);
    margin-bottom: var(--space-2);
  }

  .coming-soon-desc {
    font-size: 15px;
    color: var(--light-text-secondary);
    max-width: 460px;
    margin-inline: auto;
  }
</style>
